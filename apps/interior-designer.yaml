apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: interior-designer
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/alpar-t/homelab
    targetRevision: main
    path: config/interior-designer/manifests
  destination:
    server: https://kubernetes.default.svc
    namespace: baloo
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
