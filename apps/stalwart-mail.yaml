apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: stalwart-mail
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/alpar-t/homelab
    targetRevision: main
    path: config/stalwart-mail/manifests
  destination:
    server: https://kubernetes.default.svc
    namespace: stalwart-mail
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

