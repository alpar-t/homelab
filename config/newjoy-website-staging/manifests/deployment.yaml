# Render with ./deploy/homelab/render-staging-deployment.sh <tag@sha256:digest>.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: website
  namespace: newjoy-website-staging
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: newjoy-website
  template:
    metadata:
      labels:
        app.kubernetes.io/name: newjoy-website
    spec:
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 101
        runAsGroup: 101
        fsGroup: 101
        seccompProfile: {type: RuntimeDefault}
      containers:
        - name: nginx
          image: ghcr.io/alpar-t/newjoy-website:sha-8e97387809accd8a8b2dad1372999202391e1dce-run-128-1@sha256:384c43a11ad717f224770ed5c998726056a1c6ed9223cd77e75f24f1fd8dcf13
          imagePullPolicy: IfNotPresent
          ports:
            - {name: http, containerPort: 8080}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: {drop: [ALL]}
          resources:
            requests: {cpu: 10m, memory: 32Mi}
            limits: {cpu: 200m, memory: 128Mi}
          readinessProbe:
            httpGet: {path: /healthz, port: http}
            initialDelaySeconds: 2
            periodSeconds: 10
          livenessProbe:
            httpGet: {path: /healthz, port: http}
            initialDelaySeconds: 5
            periodSeconds: 30
          volumeMounts:
            - {name: nginx-config, mountPath: /etc/nginx/conf.d/default.conf, subPath: default.conf, readOnly: true}
            - {name: environment, mountPath: /usr/share/nginx/html/environment.json, subPath: environment.json, readOnly: true}
            - {name: cache, mountPath: /var/cache/nginx}
            - {name: run, mountPath: /var/run}
            - {name: tmp, mountPath: /tmp}
      volumes:
        - name: nginx-config
          configMap: {name: website-nginx}
        - name: environment
          configMap: {name: website-environment}
        - name: cache
          emptyDir: {}
        - name: run
          emptyDir: {}
        - name: tmp
          emptyDir: {}
