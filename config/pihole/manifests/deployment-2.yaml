apiVersion: apps/v1
kind: Deployment
metadata:
  name: pihole-2
  namespace: pihole
  labels:
    app: pihole
    instance: secondary
spec:
  replicas: 1
  strategy:
    type: Recreate  # Required for PVC
  selector:
    matchLabels:
      app: pihole
      instance: secondary
  template:
    metadata:
      labels:
        app: pihole
        instance: secondary
      annotations:
        # Auto-restart when custom DNS ConfigMap changes (requires Reloader)
        configmap.reloader.stakater.com/reload: "pihole-custom-dns,pihole-dnsmasq"
    spec:
      # Ensure primary and secondary run on different nodes
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            - labelSelector:
                matchLabels:
                  app: pihole
              topologyKey: kubernetes.io/hostname
      # DNS exposed via MetalLB LoadBalancer (192.168.1.202)
      # Use external DNS directly to avoid any circular dependency with cluster DNS
      dnsPolicy: "None"
      dnsConfig:
        nameservers:
          - 1.1.1.1
          - 8.8.8.8
      initContainers:
        - name: copy-dnsmasq-config
          image: busybox:1.37@sha256:b3255e7dfbcd10cb367af0d409747d511aeb66dfac98cf30e97e87e4207dd76f
          command: ['sh', '-c', 'cp /config/* /etc/dnsmasq.d/']
          volumeMounts:
            - name: dnsmasq-config
              mountPath: /config
            - name: dnsmasq-data
              mountPath: /etc/dnsmasq.d
      containers:
        - name: pihole
          # Pi-hole v6 - Alpine-based with REST API
          image: pihole/pihole:2025.11.1@sha256:91dc91ddd413bf461c283204558f8f21839851e9824799075a7ceff7c77eea40
          securityContext:
            capabilities:
              add:
                - NET_ADMIN
                - NET_BIND_SERVICE
          env:
            - name: TZ
              value: "Europe/Bucharest"
            # Pi-hole v6 uses FTLCONF_ prefix for configuration
            # Empty password allows passwordless access to web UI and API
            - name: FTLCONF_webserver_api_password
              value: ""
            # NOTE: dns_upstreams is NOT set here so the benchmark job can manage it via API
            # Initial upstreams will default to Cloudflare, then benchmark job optimizes
            - name: FTLCONF_dns_dnssec
              value: "true"
            # Listen on all interfaces (inside the pod)
            - name: FTLCONF_dns_listeningMode
              value: "all"
            # Web interface port
            - name: FTLCONF_webserver_port
              value: "8080"
          ports:
            - name: dns-tcp
              containerPort: 53
              protocol: TCP
            - name: dns-udp
              containerPort: 53
              protocol: UDP
            - name: http
              containerPort: 8080
              protocol: TCP
          volumeMounts:
            - name: pihole-data
              mountPath: /etc/pihole
            - name: custom-dns
              mountPath: /etc/pihole/custom.list
              subPath: custom.list
            - name: dnsmasq-data
              mountPath: /etc/dnsmasq.d
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /admin/
              port: 8080
            initialDelaySeconds: 60
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /admin/
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 5
      volumes:
        - name: pihole-data
          persistentVolumeClaim:
            claimName: pihole-2-data
        - name: custom-dns
          configMap:
            name: pihole-custom-dns
        - name: dnsmasq-data
          emptyDir: {}
        - name: dnsmasq-config
          configMap:
            name: pihole-dnsmasq
