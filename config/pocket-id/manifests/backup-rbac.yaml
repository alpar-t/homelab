# RBAC for CNPG backup credentials access
# Allows the database service account to read backup credentials for recovery operations

apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: cnpg-backup-credentials-reader
  namespace: pocket-id
rules:
  - apiGroups: [""]
    resources: ["secrets"]
    resourceNames: ["cnpg-backup-credentials"]
    verbs: ["get"]

---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: pocket-id-db-backup-credentials
  namespace: pocket-id
subjects:
  - kind: ServiceAccount
    name: pocket-id-db
    namespace: pocket-id
roleRef:
  kind: Role
  name: cnpg-backup-credentials-reader
  apiGroup: rbac.authorization.k8s.io

